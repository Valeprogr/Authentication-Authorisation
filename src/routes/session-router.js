import express, { Router } from "express";
import path from 'path';
const __dirname = path.resolve();


const router = new Router();

router.get("/login", (res,send)=>{
    res.send()
})




export default router;